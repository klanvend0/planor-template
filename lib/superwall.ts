// TODO: Re-enable Superwall when ready for production build
// Superwall requires a development build and doesn't work with Expo Go
// import Superwall from '@superwall/react-native-superwall';

export const configureSuperwall = () => {
  // Superwall is disabled for now - will be enabled in production builds
  // const apiKey = process.env.EXPO_PUBLIC_SUPERWALL_API_KEY;
  // if (apiKey) {
  //   Superwall.configure({ apiKey });
  // } else {
  //   console.warn('Superwall API key not found');
  // }
};
